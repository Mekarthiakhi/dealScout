
version: '3.9'

services:
  web:
    build: ./apps/web
    ports:
      - "5173:5173"

  api:
    build: ./apps/api
    ports:
      - "5000:5000"

  mongo:
    image: mongo

  redis:
    image: redis
